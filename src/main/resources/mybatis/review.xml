<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- dev.mvc.cate.CateDAOInter 패키지에 등록된 interface 명시,
      패키지명과 인터페이스명은 실제로 존재해야함,
      Spring이 내부적으로 자동으로 interface를 구현해줌. -->
<mapper namespace="dev.mvc.review.ReviewDAOInter">
  <!-- 
  insert: INSERT SQL 실행
  id: Spring에서 호출시 사용
  parameterType: 전달받는 데이터 객체의 타입
  return: 등록한 레코드 갯수 리턴
  SQL선언시 ';'은 삭제
  #{name}: VO class의 public String getName() 자동 호출
  #{categrpno}: VO class의 public int getCategrpno() 자동 호출
   --> 
  <insert id="create" parameterType="dev.mvc.review.ReviewVO">
    INSERT INTO review(reviewno, userno, storeno, score, contents,rdate)
    VALUES(review_seq.nextval, #{userno}, #{storeno}, #{score}, #{contents}, sysdate)
  </insert> 

<!-- 조회, id: read, 입력: reviewno, 리턴: reviewVO -->
  <!-- <select id="read" resultType="dev.mvc.reviewReviewVO" parameterType="int">
    SELECT  reviewno, content, file1, file1saved, thumb1, score, rdate
    FROM review
    WHERE reviewno=#{reviewno}
  </select> -->

<!-- 수정-->
<!-- <update id="update" parameterType="dev.mvc.review.ReviewVO">
  UPDATE review
  SET title=#{title}, contents=#{contents},  word=#{word}, 
        file1=#{file1}, file1saved=#{file1saved}, thumb1=#{thumb1}
  WHERE reviewno = #{reviewno}
</update>  -->

<!-- 삭제 -->
<!-- <delete id="delete" parameterType="int">
    DELETE FROM contents
    WHERE contentsno=#{contentsno}
  </delete> -->


</mapper>
